# Additional requirements for Intelligence System Refactoring
# Add these to your main requirements.txt

# SetFit for question classification (replaces LLM calls)
setfit>=1.0.0
sentence-transformers>=2.2.0
datasets>=2.14.0

# Training dependencies (optional, only if you plan to retrain models)
scikit-learn>=1.3.0

# Business rules engine (replaces hard-coded thresholds)
json-logic>=1.1.1

# LangChain for message storage (standardized chat history)
langchain>=0.1.0
langchain-community>=0.0.20
psycopg2-binary>=2.9.0
